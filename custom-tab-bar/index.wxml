<van-tabbar active="{{ active }}" bind:change="switchTab">
  <block wx:for="{{ tabbar }}" wx:key="index">
    <van-tabbar-item icon="home-o" class="{{ item.isCenter ? 'center-item' : '' }}">{{ item.text }}</van-tabbar-item>
  </block>
</van-tabbar>