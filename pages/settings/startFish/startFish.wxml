<!--pages/settings/startFish/startFish.wxml-->
<view class="container">
  <van-cell-group>
    <van-field label="钓位号" value="{{ form.name }}" input-align="right" data-field="name" bind:change="handleChange" clearable placeholder="请选择钓位号" />
    <!-- <van-field label="时长" value="{{ form.name }}" data-field="name" bind:change="handleChange" clearable placeholder="请选择时长" /> -->
  </van-cell-group>

  <view class="operate-btn">
    <van-button type="info" block bind:click="handleSave">保存</van-button>
  </view>
</view>